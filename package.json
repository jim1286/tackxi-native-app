{
  "name": "Tackxi-App",
  "version": "1.0.0",
  "scripts": {
    "server": "cd server && npm start",
    "client": "cd Tackxi && npm run ios",
    "start": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
